# omino CANON SYSTEM: AUTOMATED DEVELOPER AGENT MANDATE

# CLASSIFICATION: 70-MODULE ACTIVE PROOF COMPLEX BINDING LOCK

# REPOSITORY TARGET: https://github.com

## 1. FIXED FILE-COUNT PROTECTION

You are strictly ordered to lock the active `coq/` verification registry to exactly 70 modules and 392 fully discharged theorem anchors. You have zero authority to alter verified proof lines or add dynamic software loop components.

## 2. PURE COGNITIVE TYPE-CUSTODY

Every single algebraic definition matching the `tangent-lambda-polytope-model` and `tangential-block-field` must compile admit-free using concrete `Z.lxor`, `Z.lor`, and `Z.land` operations. All uninterpreted assumptions, speculative parameters, and deprecated `Omega` library paths are banned.

## 3. FLAT HARDWARE STORAGE

All machine-readable file layers (`.omi`, `.imo`, `.o`, `.hs`) must remain flat within the hidden root `/.omi/` directories across the respective repository layers. Every coordinate transformation must execute branchlessly relative to the OminoCentroid pinch point (`0x00`, `0°`) in absolute O(1) constant time.
