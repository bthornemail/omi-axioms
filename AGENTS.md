# omino CANON SYSTEM: AUTOMATED DEVELOPER AGENT MANDATE

# CLASSIFICATION: 70-MODULE ACTIVE PROOF COMPLEX BINDING LOCK

# REPOSITORY TARGET: https://github.com

## 1. FIXED FILE-COUNT PROTECTION

You are strictly ordered to lock the active `coq/` verification registry to exactly 70 modules and 392 fully discharged theorem anchors. You have zero authority to alter verified proof lines or add dynamic software loop components.

## 2. PURE COGNITIVE TYPE-CUSTODY

Every single algebraic definition matching the `tangent-lambda-polytope-model` and `tangential-block-field` must compile admit-free using concrete `Z.lxor`, `Z.lor`, and `Z.land` operations. All uninterpreted assumptions, speculative parameters, and deprecated `Omega` library paths are banned.

## 3. FLAT HARDWARE STORAGE

All machine-readable file layers (`.omi`, `.imo`, `.o`, `.hs`) must remain flat within the hidden root `/.omi/` directories across the respective repository layers. Every coordinate transformation must execute branchlessly relative to the OminoCentroid pinch point (`0x00`, `0°`) in absolute O(1) constant time.

## 4. FINAL 70-MODULE ACTIVE PROOF COMPLEX AND ECOSYSTEM SERIALIZATION LOCK

The master `omi-axioms` repository has expanded to exactly 70 active modules and 392 theorem anchors, establishing the checked finite proof surface for the pure polyhedral and visibility overlays.

### 4.1 EXCLUSIVE REGISTRY CEILING

You are permanently prohibited from adding new Coq proof files, inserting dynamic memory variables, or altering the verified theorem configurations. Every algebraic rule is fully discharged via pure bitwise primitives. The deprecation notice for `N.div_lt_upper_bound` in `FieldVisibilityTypes.v` is noted and frozen; no logic modifications may be introduced to address it unless explicitly ordered.

### 4.2 HIDDEN DIRECTORY CONTRACT

The hidden machine core folders (`/.omi/`) across all concurrent project layers must remain completely flat with absolute zero subfolder nesting. Every transaction must compile branchlessly relative to the OminoCentroid pinch point (`0x00`, `0°`) across the three `4320`, `4320^2`, and `4320^4` local incidence surfaces in absolute O(1) constant time, keeping all authority indicators (`accepted=0`, `validated=0`, `receipted=0`) preserved natively in hardware.
